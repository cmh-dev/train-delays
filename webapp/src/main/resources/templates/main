<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <title>Train Times</title>
</head>
<body>
    <table>
        <tr th:each="trainService : ${model.trainServiceInfo.trainServices}">
            <td th:text="${trainService}"></td>
        </tr>
    </table>
</body>
</html>